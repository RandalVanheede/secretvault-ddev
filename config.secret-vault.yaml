#ddev-generated
## Description: Secret vault hooks — inject on start, clean on stop
hooks:
  pre-start:
    - exec-host: "bash ${DDEV_APPROOT}/.ddev/secret-vault/update-check.sh"
    - exec-host: "bash ${DDEV_APPROOT}/.ddev/commands/host/vault inject --quiet"
  post-stop:
    - exec-host: "bash ${DDEV_APPROOT}/.ddev/commands/host/vault clean --quiet"
